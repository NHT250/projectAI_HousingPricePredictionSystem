# Frontend URL allowed by FastAPI CORS (comma separated if multiple)
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# MongoDB configuration for backend
MONGODB_URI="mongodb://localhost:27017"
MONGODB_DB="real_estate_app"

# Path to trained sklearn model artifact
MODEL_PATH="backend/artifacts/house_price_model.joblib"
